apiVersion: v1
kind: Pod
metadata:
  labels:
    app: myapp
  name: test
spec:
  containers:
  - image: 966145/myserver121:latest
    name: con1
    ports:
    - containerPort: 80
  - name: con2
    image: tomcat:latest
    ports:
    - containerPort: 8080
---
#apiVersion: v1
#kind: Service
#metadata:
#  name: test-svc
#spec:
#  ports:
#  - port: 80
#    nodePort: 30123
#  selector:
#    app: myapp
#  type: NodePort
